# OKX DEX Aggregator Skill

Professional trading and market data tools via OKX Web3 API.

## Available Modules

Select your desired capabilities when adding this skill:

1. **Quote Module** (`skills/quote`): Real-time pricing and optimal routing.
2. **Swap Module** (`skills/swap`): On-chain execution with local signing (Solana Specialist).
3. **Market Module** (`skills/market`): Token metadata and market price intelligence.

---
metadata: { "openclaw": { "requires": { "env": ["OKX_DEX_API_KEY", "OKX_DEX_SECRET_KEY", "OKX_DEX_PASSPHRASE", "SOLANA_PRIVATE_KEY"] } } }
